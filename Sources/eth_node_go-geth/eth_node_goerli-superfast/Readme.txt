"Superfast" is a description of the geth node running in "SNAP" mode.
The node downloads all blocks (including headers, transactions and receipts), verifies all headers, and downloads the state and verifies it against the headers.
It uses a faster algorythm than the "FAST" node.